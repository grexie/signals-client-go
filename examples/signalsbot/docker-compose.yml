services:
  signalsbot:
    build:
      context: ../..
      dockerfile: examples/signalsbot/Dockerfile
    env_file:
      - .env
    environment:
      SIGNALS_DB_PATH: /data/signalsbot.db
    volumes:
      - signalsbot-data:/data
    restart: unless-stopped

volumes:
  signalsbot-data:
